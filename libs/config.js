
let config = {};

config.port = 3093;
config.brand = '#BlondieCode';
config.filesLink = '/statics';
config.dirs = {
    publicDir: 'public',
    uploadDir: 'uploads',
    filesDir: 'files'
};

module.exports = config;